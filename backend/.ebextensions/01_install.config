commands:
  01_install_deps:
    command: "npm install --production"
    cwd: "/var/app/current"
    leader_only: true

option_settings:
  aws:elasticbeanstalk:container:nodejs:
    ProxyServer: nginx
